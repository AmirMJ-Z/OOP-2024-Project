package model.GameComparators;

import model.Game;

import java.util.Comparator;

public class GameComparatorLevel implements Comparator<Game> {
    @Override
    public int compare(Game o1, Game o2) {
        return 0;
    }
}
